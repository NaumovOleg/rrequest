---
phase: 07-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/extension/extension.ts
  - src/webview/views/Sidebar/Sidebar.tsx
  - src/webview/state/store.ts
autonomous: true
must_haves:
  truths:
    - User can send the active request from anywhere in VS Code via Cmd/Ctrl+Enter
    - User can switch the active environment from the Command Palette
    - User can import cURL without opening the sidebar
    - User can open a new request / duplicate from the Command Palette
  artifacts:
    - package.json keybindings block
    - package.json commands (rrequest.send, rrequest.changeEnvironment, rrequest.importCurl, rrequest.newRequest)
    - extension.ts command handlers
  key_links:
    - webview keybinding -> postToHost({type:'sendRequest'})
    - palette command -> same send path as the Send button
---

<objective>
Keyboard-first daily usage: add VS Code Command Palette entries + keybindings for
the most common actions, wired to the same message paths already used by the
clickable UI.

Purpose: "send / import / switch env" should never require a mouse.
Output: 4 new palette commands with default keybindings, one shared send path.
</objective>

<execution_context>
The Send button posts {type:'sendRequest', requestId, payload} to the host via
postToHost in src/webview/ipc.ts. EditorApp receives 'response' events and
lives the store. Search existing patterns: RequestPanel.tsx:522 already handles
Cmd/Ctrl+Enter for send — the host handler must be re-entrant the same way.
</execution_context>

<context>
@src/extension/extension.ts
@package.json
@src/extension/messaging.ts (sendRequest case at line 159)
@src/webview/views/RequestPanel/RequestPanel.tsx (keydown handler at ~522)
</context>

<tasks>

<task type="auto">
  <name>Add palette commands + keybindings</name>
  <files>package.json, src/extension/extension.ts</files>
  <action>In package.json contributes.commands add:
    rrequest.newRequest ("RREQUEST: New Request"),
    rrequest.changeEnvironment ("RREQUEST: Change Environment"),
    rrequest.importCurl ["RREQUEST: Import cURL from clipboard"],
    rrequest.openDocs stays.
  Add contributes.keybindings: cmd+enter -> rrequest.sendRequest; cmd+e -> rrequest.respond; cmd+shift+r -> rrequest.runLastRequest. Keep existing commands untouched. In extension.ts registerCommand each new command: newRequest opens blank editor tab via the same code path as sidebar "+" button (find it: it posts {type:'openInEditor'?} — reuse the blank-request open helper from panel/sidebar-view); changeEnvironment sends ('loadEnvironments') message to webview / or simpler: executes the same IPC the EnvDropdown uses; importCurl reads vscode.env.clipboard, latin parses via parseCurl (src/webview/curl.ts) — but parseCurl lives in webview, so host must re-implement or import shared copy: if parseCurl is only a webview module, move/extract minimal curl parsing into src/shared/curl.ts (import in both webview and extension) then post the parsed request as an editor open.</action>
  <verify>yarn run typecheck (webview+extension compile), launch extension host (F5), run Command Palette search for each new title; paste a curl into clipboard and 'Import cURL' opens a populated tab.</verify>
  <done>Palette shows 4 new entries; each triggers the right panel/tab; send works via cmd+enter without touching the mouse; no TS errors.</done>
</task>

<task type="auto">
  <name>Share curl parser between webview and host</name>
  <files>src/shared/curl.ts (new), src/webview/curl.ts (maybe move or re-export), src/extension/extension.ts</files>
  <action>If parseCurl must run host-side, relocate its implementation to src/shared/curl.ts (both webview + extension import from it; keep public API identical). If webview is the only consumer, instead host posts {type:'openInEditor'} with a zeroed tab and the webview imports from clipboard — prefer the FIRST approach only if host-side import /add is needed; otherwise skip this task and keep all parsing in webview (decide at implementation time, minimal diff wins). When relocating, keep pure functions to be unit-testable.</action>
  <verify>yarn test passes; webview keeps compiling; import cURL still populates fields in both old UI path and new palette command.</verify>
  <done>One parser exported from shared, used by both surfaces; no duplicated logic.</done>
</task>

</tasks>

<verification>
- yarn run typecheck
- yarn test
- Manual: F5, palette each entry, cmd+enter sends request, cmd+e imports curl.
</verification>

<success_criteria>
Command palette: New Request, Change Environment, Import cURL all functional with default keybindings "when" clauses scoped so they don't conflict with editor defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux/07-01-SUMMARY.md` in the phase dir.
</output>