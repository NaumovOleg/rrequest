---
phase: 08-power
plan: 07
type: execute
wave: 3
depends_on: [04,06]
files_modified:
  - src/shared/types.ts
  - src/extension/messaging.ts
  - src/webview/views/RequestPanel/RequestPanel.tsx
  - src/webview/views/Sidebar/Sidebar.tsx
  - package.json
  - src/extension/extension.ts
autonomous: true
must_haves:
  truths:
    - Method is a free-text input with a dropdown of presets (custom methods like PROPFIND work)
    - Viewer(read-only) role: editor inputs are locked, not silently accepted
    - Empty states for sidebar and editor guide the user to create the first request
    - Onboarding: first-run diff view with a starter sample request+collection delivered as requested artifact
  artifacts:
    - method dropdown with editing + preset list
    - readOnly flag propagated into webview; disabled props on relevant inputs
    - empty-state components; sample collection on first run
  key_links:
    - method select -> input with datalist/popup
    - deps.isReadOnly -> webs

---

<objective>
Diamond UX polish catch-all: free-form method editing, definitive viewer lock,
empty states, onboarding with a sample collection, plus earlier discussed
keyboard-first tweaks (excluding resend-from-history, which is skipped by
decision). All small, high-frequency.

Purpose: polish items from pad report.
</objective>

<execution_context>
- Method is a <select> in RequestPanel (RequestPanel URL bar). Change to
  combobox: input + dropdown of presets, splitting on blur; the existing
  HttpMethod type already allows string & {}.
- readOnly: messaging.ts guard exists (isMutating -> toast). The editor tab is
  opened from collection; need to pass readOnly into the webview state (store
  flag via 'panels' host message or reuse existing workspaces state — check
  panel bootstrap; simplest: include role in the 'render' bootstrap message that
  both editor and sidebar already receive, then store.role gates disabled states.
- Empty state: sidebar flabel when collections=[] (illustration + create button
  currently exists? Sidebar has New HTTP Request button always visible — add
  text hint "No collections yet — create one or import" + import button).
  Editor empty state after open with no request: show "New Request" CTA.
- Onboarding: on first extension activation (flag in globalState), create a
  sample collection named 'Try RREQUEST' with 2 requests (a GET + a POST with
  JSON) pointing to https://jsonplaceholder.typicode.com/todos/1 etc. — only
  when workspace collection list is empty AND flag not set. One-time.
</execution_context>

<context>
@src/webview/views/RequestPanel/RequestPanel.tsx (method select, URL area)
@src/webview/views/Sidebar/Sidebar.tsx (empty state area, header)
@src/webview/state/store.ts (readOnly state)
@src/extension/messaging.ts (isReadOnly)
@src/extension/extension.ts (activation, workspace store)
</context>

<tasks>

<task type="auto">
  <name>Method combobox + read-only lock</name>
  <files>src/webview/views/RequestPanel/RequestPanel.tsx, src/webview/state/store.ts, src/extension/messaging.ts, src/extension/panel.ts</files>
  <action>Method: replace <select> with combobox (custom small component or native input + <datalist>; datalist keeps native UX and zero code). options list: existing 8 + PROPFIND, M-SEARCH, QUERY stays. Value editable; on send, HttpMethod accepts arbitrary string.
  readOnly: store gets isReadOnly from bootstrap message (check what panel bootstrap sends today; if absent add 'readOnly' in HostMessage for editor bootstrap + sidebar). All editable fields in RequestPanel (url, params, headers, body textareas, auth fields, scripts, save button) disabled when store.isReadOnly? Only whether viewer role — readOnly vs 'owner/editor/col? isReadOnly yes -> force). Keep send but no persist? README says: viewer can execute but not mutate collections — disable only persisting fields (Save button hidden), inputs stay editable for experimentation, matching current prod? That is current behavior; the polish is q: the plan says "not silently accepted" — make it obvious: disable Save + show banner “Read-only workspace — changes won't be saved” red/protection. Implement that: Save disabled + banner.
  </action>
  <verify>yarn test (helper changes); F5 in viewer role workspace: Save disabled, banner shown; custom method typed sends with that WWW verb; keyboard: typing in method input doesn't trigger send. </verify>
  <done>Method combobox works; viewer locks clearly communicated, no silent no-op.</done>
</task>

<task type="auto">
  <name>Empty states + onboarding sample collection</name>
  <files>src/extension/extension.ts, src/webview/views/Sidebar/Sidebar.tsx, package.json</files>
  <action>extension.ts on activate: if workspaces list empty? No: check 'rrequest.onboarded' in globalState; if unset: create workspace 'Default' (already auto), then seed one collection 'RREQUEST examples' with 2 requests (GET https://jsonplaceholder.typicode.com/todos/1, POST https://jsonplaceholder.typicode.com/posts with JSON), set flag. Guarded: only if user isn't in an imported flow? Simple guard: collections empty globally.
  Sidebar: when collections array empty, render empty-state panel (icon + text “No collections yet. Create or import.”) with buttons: [+ New HTTP Request], [Import]. Replaces bare rows.
  Also progress bar spacing small.
  </action>
  <verify>F5 fresh: sample collection exists after first activation; delete all collections → empty state with buttons; F5 again → no duplicate seeds (flag).</verify>
  <done>Empty state present; onboarding seeds once; import path available.</done>
</task>

</tasks>

<verification>
- yarn typecheck && yarn test
- F5 manual per tasks.
</verification>

<success>
Custom methods work, viewer lock is visible, onboarding seeds the sample once,
empty sidebar is inviting.
</success>

<output>
08-07-SUMMARY.md
</output>