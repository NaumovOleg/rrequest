---
phase: 08-power
plan: 08
type: execute
wave: 2-3
depends_on: []
files_modified:
  - src/webview/views/Sidebar/Sidebar.tsx
  - src/extension/messaging.ts
  - src/shared/types.ts
autonomous: true
must_haves:
  truths:
    - User can shift/ctrl-click multiple requests/folders/collections and delete, duplicate or move them in bulk
    - Batch ops respect viewer role (read-only → blocked with toast)
  artifacts:
    - multiSelect state in sidebar store
    - batch messages in WebviewMessage/HostMessage
  key_links:
    - selectionState in Sidebar; deleteSelected -> deleteRequest loop w/ per-item cascade
---

<objective>
Bulk operations in the collections tree: multi-select (cmd/ctrl+click,
shift-range) with a floating action row: Delete → Trash (per-item, keeps
current damage semantics), Duplicate (in place), Move to collection (popup).
Ponytail note: no reorder/export bulk; just the three ops users actually need.

Purpose: close "no bulk operations" gap from inventory; cheap win.
</objective>

<execution_context>
- Sidebar.tsx renders rows; selection state today: single-selected? Add
  selectedIds in store (zustand). Rows currently have dnd + context menu;
  extend row onMouseDown to support modifiers. The trash delete path already
  exists per item (deleteRequest/deleteCollection/deleteFolder cases) — batch =
  sequential loop in webview, no new host logic except maybe a batch delete
  message? Keep host untouched: fire N messages (existing throttles? fine for
  handful; cap 100 items with warning).
- ContextMenu already exists; add bulk actions bar at tree bottom when
  selection>1.
- 'Move to' dialog: reuse existing move popup (ContextMenu/PopupMenu pattern).
</execution_context>

<context>
@src/webview/views/Sidebar/Sidebar.tsx
@src/webview/components/ContextMenu.tsx, PopupMenu.tsx
@src/webview/state/store.ts
@src/shared/types.ts (WebviewMessage)
</context>

<tasks>

<task type="auto">
  <name>Multi-select + bulk action bar</name>
  <files>src/webview/views/Sidebar/Sidebar.tsx, src/webview/state/store.ts</files>
  <action>
  store: selectedIds: Set<string> (serialized: object map), toggleSelect(id, additive), clearSelection.
  Sidebar rows: onMouseDown with cmd/ctrl or shift → additive (collect); plain click → single + clear. On Blur/empty click outside → clear.
  Action bar (fixed bottom of tree): "N selected · [Delete] [Duplicate] [Move…]" — Delete → for each selected id: call the matching delete (kind-aware: request/folder/collection; same icons as row) → trash behavior; Duplicate → duplicateRequest each; Move → Popup with collections (+ folders) dest, then moveRequest/moveFolder per item.
  Selection breaks: rows that are folder/collection mixed — invalid combos blocked with toast.
  Careful: dragging with selection stays single-draggable (no drag-multi; skip).
  </action>
  <verify>F5: seed 5 requests, shift-click all, delete → all in Trash; undo via restore; duplicate batch; move batch from urgency popup.</verify>
</task>

</tasks>

<verification>
- yarn typecheck && yarn test (UI types compile; no host change so no new tests needed unless store tests exist)
- F5 manual flow as above; viewer role: actions disabled with existing toast.
</verification>

<success>
>0</success>?</lang>

<output>
08-08-SUMMARY.md
</output>