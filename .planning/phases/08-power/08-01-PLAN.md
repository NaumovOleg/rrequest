---
phase: 08-power
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/extension/extension.ts
  - src/extension/panel.ts
  - src/extension/net/oauth2.ts (new)
  - src/extension/messaging.ts
  - src/webview/views/RequestPanel/RequestPanel.tsx
autonomous: true
must_haves:
  truths:
    - User can configure OAuth2 authorization-code (PKCE) and client-credentials grants in the request Auth tab
    - Token is fetched via a localhost callback server, stored in VS Code Secret Storage, and re-used on subsequent sends
    - Refresh token flow reissues the token before expiry automatically
  artifacts:
    - oauth2 auth type in Auth union
    - net/oauth2.ts with authorize()/clientCredentials()/refresh()
    - AuthEditor UI fields for the two grants
    - secret storage key per request id
  key_links:
    - request send -> resolveOAuthToken() -> Authorization header
    - Auth tab -> "Get token" button -> vscode.env.openExternal + local callback
---

<objective>
Add OAuth 2.0 support: Authorization Code with PKCE and Client Credentials
grants. The token (and refresh token) live in VS Code Secret Storage, never in
the workspace JSON, so sync/export never leaks them. Tokens are minted on
demand, cached per request, and refreshed automatically when expired.

Purpose: remove the most-requested auth gap vs Postman/Insomnia.
Output: working OAuth2 tab, token injection into the Authorization header,
persisted secrets.
</objective>

<execution_context>
- Auth is applied host-side in src/extension/net/http-client.ts applyAuth()
  (line ~46): extend to call oauth resolve before building headers.
- Messaging `sendRequest` (messaging.ts:178) is the single send path; hook
  token resolution there so both palette send and button send work.
- Existing defaults pattern: package has no OAuth today; types.ts Auth is a
  closed union — extend it with the oauth2 variant.
- No new dependency: use node:http for the loopback server, node:crypto for
  PKCE (sha256), vscode.env.openExternal for the browser.
</execution_context>

<context>
@src/shared/types.ts (Auth union, RestRequest)
@src/extension/net/http-client.ts (applyAuth)
@src/extension/messaging.ts (sendRequest case ~178)
@src/extension/panel.ts (deps wiring ~413, secrets ~227)
@src/webview/views/RequestPanel/RequestPanel.tsx (AuthEditor section)
@src/extension/extension.ts (command registration)
</context>

<tasks>

<task type="auto">
  <name>OAuth2 types + host-side client</name>
  <files>src/shared/types.ts, src/extension/net/oauth2.ts (new)</files>
  <action>types.ts: extend Auth union:
    { type:'oauth2'; grant:'authorization-code'|'client-credentials'; authUrl; tokenUrl; clientId; clientSecret?; scope?; tokenKey?: string }
    (tokenKey is the Secret Storage key owning the stored token — UI never sets it.)
  oauth2.ts exports:
    - pkceChallenge(): {verifier, challenge}
    - authorize(auth, redirectUri): launches node:http server on 127.0.0.1 ephemeral port, opens redirect_uri=... in browser via injected openExternal, resolves {code} from the callback URL, validates state; exchanges via fetch POST tokenUrl (application/x-www-form-urlencoded): grant_type=authorization_code, code, redirect_uri, client_id, code_verifier (PKCE), client_secret if set.
    - clientCredentials(auth): POST tokenUrl grant_type=client_credentials, client_id, client_secret, scope.
    - refresh(auth, refreshToken): grant_type=refresh_token.
    All return {accessToken, refreshToken?, expiresIn?}. Errors as Error with human message. Abort after 60s (user closes the tab) — reject with 'Aborted'.
  For tests: keep functions dependency-injected (openExternal, fetch) so vitest can stub them.</action>
  <verify>yarn test: new unit tests for pkceChallenge (RFC 7636: verifier 43-128 chars, challenge = base64url(sha256(verifier))), and for token exchange builders with a stubbed fetch (assert body encoding + Authorization header absent).</verify>
  <done>Types compile; oauth2.ts exports tested functions; no TS errors.</done>
</task>

<task type="auto">
  <name>Send path + Secret Storage + wiring</name>
  <files>src/extension/messaging.ts, src/extension/panel.ts, src/extension/extension.ts</files>
  <action>
  - panel.ts deps: add oauth2 module (pass context.secrets + vs.env-ish openExternal through deps; follow existing deps pattern at line ~413).
  - messaging.ts, in sendRequest case, BEFORE running scripts: if payload.auth?.type === 'oauth2' at each level, run ensureToken(auth):
      tokenKey = secrets key 'rrequest.oauth.' + requestId (or reuse via auth.tokenKey persisted per request? No: derive from request id).
      Load from secrets; if access token present and not expired (store {t, exp} JSON payload), return it.
      Else if grant authorization-code: refresh via refreshToken if stored, else error (user must click "Get token" in UI).
      Client-credentials: always fetch a fresh one (cheap, stateless) — cache for 5 min in a module Map keyed by requestId.
      Write returned token JSON back to secrets.
    Then set Authorization: Bearer <token> into the request headers unless user already set an authorization header (matching http-client behavior).
  - Also handle wsSession: this is HTTP only (WS/gRPC keep their current auth as manual headers; note in plan SUMMARY as known limitation).
  - extension.ts: register command 'rrequest.oauthToken' (RREQUEST: Fetch OAuth token) that finds the active request tab (host tracks active request id from setTitle? Simpler: post a message to the editor webview asking it to run the flow) — decision: implement UI-driven instead: the AuthEditor button posts {type:'oauthGetToken', requestId} to host, host runs authorize() with the CURRENT tab payload, stores token, replies {type:'oauthToken', ok, error} => webview shows toast. No palette command needed (YAGNI).
  - messaging.ts new case 'oauthGetToken': read auth config from msg, call the right grant, store token, return ok/error. Redirect URI is 127.0.0.1 ephemeral port (host prints it into the browser URL).
  types.ts WebviewMessage/HostMessage: add oauthGetToken + oauthResult.
  </action>
  <verify>yarn typecheck; unit test: fake fetch + fake openExternal+intercepted callback -> authorize() returns token; secrets store called; messaging route returns oauthResult.
  Manual (F5): with a test OAuth server (or https://api.github.com-style token endpoint) run the full flow: Auth tab -> choose OAuth2 -> fill fields -> "Get token" -> browser opens -> approve -> token appears, send request carries Authorization.
  </verify>
  <done>OAuth token flow works end to end; token never leaks into collections JSON; refresh happens silently.</done>
</task>

<task type="auto">
  <name>AuthEditor OAuth2 UI</name>
  <files>src/webview/views/RequestPanel/RequestPanel.tsx</files>
  <action>In the Auth section, add type option 'OAuth2'. When selected show grant radio: Authorization Code (PKCE) / Client Credentials.
  Fields: Authorization URL, Access Token URL, Client ID, Client Secret (password input), Scope (comma-separated). For client-credentials hide authUrl+scope is allowed (keep field hidden).
  Buttons: [Get token] (posts oauthGetToken), and show status line: "Token stored (expires in Xs)" read from last oauthResult. Changing any field clears the cached status (token may be stale).
  Keep the existing none/bearer/basic/apikey code untouched.</action>
  <verify>F5: Auth tab shows OAuth2 fields; Get token triggers host flow; toast on success/error; switching grants toggles fields.</verify>
  <done>UI matches the host API; no regressions in other auth types.</done>
</task>

</tasks>

<verification>
- yarn typecheck && yarn test
- F5 manual: set up a throwaway OAuth server (e.g. local test fixture via node:http) with /auth + /token; run code flow; refresh flow when the access token expires (use small expiresIn fixture); client-credentials flow.
- Trash/export: request JSON in workspace file must NOT contain the token, only tokenKey (assert while debugging with workspace file open).
</verification>

<success_criteria>
OAuth2 authorization-code (PKCE) and client-credentials work end-to-end in the
extension; tokens cached per request and refreshed; no token material in the
workspace JSON file, sync snapshots, or Postman export.
</success_criteria>

<output>
After completion, create `.planning/phases/08-power/08-01-SUMMARY.md`.
</output>