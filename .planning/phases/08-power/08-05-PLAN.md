---
phase: 08-power
plan: 05
type: execute
wave: 2-3
depends_on: [01]
files_modified:
  - src/extension/net/graphql.ts (new)
  - src/extension/messaging.ts
  - src/webview/views/RequestPanel/RequestPanel.tsx (Body/GraphQL tab)
  - src/webview/state/store.ts
autonomous: true
must_haves:
  truths:
    - User can introspect a GraphQL endpoint and see the resulting schema
    - Schema autocomplete / field hints are available while typing a query (dictionary-level, not full LSP)
  artifacts:
    - introspection client (POST to url, reuse request headers/auth? minimal: use the request's own config via sendRequest-less approach)
    - schema cache per workspace (JSON in globalState) + queries list
  key_links:
    - introspect message -> host fetch -> schema -> webview store
    - typing in query textarea -> local schema autocomplete popup
---

<objective>
GraphQL schema explorer: introspect an endpoint from the request's GraphQL
body tab, store the schema per workspace so it survives reloads, and offer
field-name autocomplete in the query editor. No LSP — a simple dictionary from
the introspected types.

Purpose: Insomnia/Postman parity on the strongest GraphQL workflow, without
heavy machinery.
</objective>

<execution_context>
- HTTP is already built; GraphQL mode posts JSON {query, variables} with the
  request's headers/auth — reuse by building a RestRequest variant from the
  GS request (body.mode=graphql with the introspection query (graphql-schema
  standard introspector text), method POST.
- Schema cached per workspace in panel/globalState (simple JSON map keyed by
  endpoint URL + workspace id) so it survives panel close.
- UI integration lives in RequestPanel's body tab where GraphQL body is edited
  (see RequestPanel Body section for graphql mode).
</execution_context>

<context>
@src/extension/net/http-client.ts (sendRequest reuse)
@src/extension/messaging.ts (sendRequest graphql body build)
@src/webview/views/RequestPanel/RequestPanel.tsx (graphql body textareas)
@src/webview/state/store.ts (state shape additions)
</context>

<tasks>

<task type="auto">
  <name>Introspection host client + cache</name>
  <files>src/extension/net/graphql.ts (new), src/extension/messaging.ts, src/extension/panel.ts</files>
  <action>graphql.ts:
  - INTROSPECTION_QUERY constant (standard introspection schema query returning __schema).
  - async introspect(url, headers, body:{query,variables}, auth) -> {types} tree filtered to user-relevant: for each type collect {name, kind, fields[{name,type}]}. Preserve @deprecated fields as-is.
  - cacheStore: map endpoint→schema persisted via panel globalState JSON (small; items: {url, types, at}).
  messaging.ts: case 'graphqlIntrospect' {url, headers?, auth?} -> introspect via sendRequest with introspection query; on ok persist cache + return {types}; case 'graphqlSchema' for loading cached.
  panel.ts: init cache namespace 'rrequest.graphql'.
  </action>
  <verify>yarn test: introspect against a fake fetch-stub (mock introspection JSON), assert types+fields extracted and empty on malformed response.</verify>
  <done>Host introspection solid; cache survives restart.</done>
</task>

<task type="auto">
  <name>UI: introspection button + autocomplete</name>
  <files>src/webview/views/RequestPanel/RequestPanel.tsx, src/webview/state/store.ts</files>
  <action>In GraphQL body section add toolbar: [Introspect] (sends graphqlIntrospect with active request config; disables while running; error toast on failure), [Schema] icon opens side panel listing types (expandable) — clicking a type name inserts its field list snippet into query at caret via simple text insertion at current cursor pos.
  Autocomplete: cache schema in store; while typing in query textarea (onChange) match last word before caret against schema field names (types keys + field names) — render small popup list; Enter/Tab inserts. Cap to 20 matches. Simple, no codemirror/monaco.
  If no schema cached: Introspect button emphasized; auto-introspect on first open of GraphQL mode? No — explicit button only (YAGNI server hit).
  </action>
  <verify>F5: introspect local server (e.g. a tiny graphql server fixture), schema panels open, click type inserts field, autocomplete triggers on `{` or 2-3 letters after `{`.</verify>
</task>

</tasks>

<verification>
- yarn typecheck && yarn test
- F5: introspection against a small local GraphQL server + autocomplete + cache reload.
</verification>

<success>
GraphQL schema introspection + field autocomplete within the existing body tab;
cache persists; no new deps.
</success>

<output>
08-05-SUMMARY.md
</output>