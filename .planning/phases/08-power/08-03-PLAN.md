---
phase: 08-power
plan: 03
type: execute
wave: 1
depends_on: [02]
files_modified:
  - src/shared/types.ts
  - src/extension/net/sse-client.ts (new)
  - src/extension/messaging.ts
  - src/webview/views/WebSocket/WebSocketPanel.tsx (or new SsePanel)
  - src/webview/editor/EditorApp.tsx
autonomous: true
must_haves:
  truths:
    - User can open an SSE stream, see incoming events with IDs/types, and stop it
    - WebSocket panel keeps all current behavior; at least URL/header interpolation from plan 02 applies
  artifacts:
    - sse host client (line reader over fetch.ReadableStream with event parsing)
    - UI panel mode for SSE (or reuse WS panel with a type toggle)
  key_links:
    - sseConnect message -> sse-client -> sseEvent/sseClosed to webview
---

<objective>
Add Server-Sent Events support: a lightweight panel for consuming
text/event-stream endpoints (event:, data:, id:, retry:) with live event log,
plus a small WS polish: same list-style event view. No new deps — use
ReadableStream parsing.

Purpose: match Hoppscotch/Postman multi-protocol default in 2026.
Output: SSE tab + host client + tests.
</objective>

<execution_context>
- WS host client already streams via ws-manager with emitted HostMessages
  (wsMessage etc.). SSE is a fetch-based stream; a new sse-client that loops
  resp.body reader and splits on \n\n events, emitting {type:'sseEvent'} +
  {type:'sseClosed'} host messages; single connection per panel id.
- The panel already has a store mode switch (EditorApp switches by store flags
  — see WebSocket/Grpc panels); add SSEMode alongside, reusing layout patterns
  of WebSocketPanel (url input, env vars, log list, connect/disconnect).
- message protocol: add {type:'sseConnect'}|{sseDisconnect}|{sseSend?} — SSE is
  GET-only so no send; plus host messages sseEvent/sseClosed/sseError.
</execution_context>

<context>
@src/webview/views/WebSocket/WebSocketPanel.tsx
@src/webview/editor/EditorApp.tsx
@src/extension/messaging.ts (ws cases)
@src/extension/net/ws-manager.ts
@src/shared/types.ts (WebviewMessage/HostMessage switches)
@src/extension/panel.ts (deps wiring)
</context>

<tasks>

<task type="auto">
  <name>Host SSE client</name>
  <files>src/extension/net/sse-client.ts (new), src/extension/messaging.ts, src/extension/panel.ts</files>
  <action>sse-client.ts: class SseClient with connect(connId, url, headers, emit, signal?) using global fetch:
  - fetch with Accept: text/event-stream; read via resp.body.getReader(), decode chunks with TextDecoder (stream:true).
  - Accumulate buffer; split on '\n\n' (and '\r\n\r\n'); for each block parse lines: 'event:', 'data:', 'id:', 'retry:'; dispatch {type:'sseEvent', connId, event: <event||message>, data: joined data lines, id?, at:Date.now()}.
  - on stream end/abort: emit {type:'sseClosed', connId, reason}.
  - Abort via injected signal: SseClient.connect returns () => abort() so messaging can cancel on disconnect message.
  messaging.ts: cases 'sseConnect'(url+headers interpolated per plan 02 activeVars), 'sseDisconnect'(abort). panel.ts: pass sse client dep.
  types.ts: add WebviewMessage 'sseConnect'|'sseDisconnect', HostMessage 'sseEvent'|'sseClosed'.
  </action>
  <verify>yarn test: unit test with a fake fetch returning a ReadableStream emitting two events ('data: a\ndata: b\n\n' etc.) asserts 2 sseEvent broadcasts + closed on end; abort test. No ws import.</verify>
  <done>Host parses SSE correctly; emits events; signal-driven close.</done>
</task>

<task type="auto">
  <name>SSE UI tab</name>
  <files>src/webview/views/Sse/SsePanel.tsx (new), src/webview/editor/EditorApp.tsx, src/webview/views/Sidebar/Sidebar.tsx (maybe just open command)</files>
  <action>New panel mode 'sse' in store/EditorApp; SsePanel mirrors WebSocketPanel: url input + Connect/Disconnect button, event log list (id/time/event/data, JSON pretty when parseable), clear button, scroll to bottom on new event, auto-connect disabled by default
.
  Wire 'openSse' message -> editor switch (reuse openWebsocket path pattern in sidebar/menus: request kind 'sse' added to CollectionItem? NO — keep SSE as an ad hoc tab like WS/grpc currently are (they have types in CollectionItem though). Scope: SSE gets a panel + a sidebar "+ New SSE" action; not persisted in collections (YAGNI; note in SUMMARY).
  </verify>
  <done>SSE mode works end to end: connect, live events, disconnect, no echo late events.</done>
</task>

</tasks>

<verification>
- yarn typecheck && yarn test (sse-client tests green)
- F5 manual vs a local SSE endpoint (node script serving text/event-stream): connect, receive events, stop; disconnect cleans up.
</verification>

<success>
SSE tab consumes an event stream live in the editor; host client tested; no new
dependencies.
</success>

<output>
08-03-SUMMARY.md
</output>