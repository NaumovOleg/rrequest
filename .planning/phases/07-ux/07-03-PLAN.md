---
phase: 07-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/webview/codegen.ts (new)
  - src/webview/views/RequestPanel/RequestPanel.tsx
  - src/webview/theme.css
autonomous: true
must_haves:
  truths:
    - User can see the active request as code (cURL, JS fetch, Python, Go)
    - Snippet updates live as the request fields change
    - One-click copy per language
  artifacts:
    - codegen.ts pure generator functions
    - Code sub-tab in RequestPanel section bar
  key_links:
    - request state -> codegen render (re-renders on edit)
---

<objective>
Add a "Code" section to the request editor that renders the active request as
ready-to-paste snippets in several languages, updating live.

Purpose: share/triage requests in Slack, bug reports, and other tools without
screenshotting the UI. Matches Thunder Client/Postman "Code" tab.
Output: `codegen.ts` (pure, testable) + Code sub-tab UI.
</objective>

<execution_context>
curl.ts already has toCurl() for cURL output. Add jsFetch/ pythonRequests / goHTTP
generators in a new src/webview/codegen.ts reusing the request shape. The sub-tab
bar pattern lives in RequestPanel.tsx (sub === 'params'|'headers'|'body'|'auth'|'tests');
extend it with 'code'. Render read-only <pre> + copy button.
</execution_context>

<context>
@src/webview/curl.ts (toCurl)
@src/webview/views/RequestPanel/RequestPanel.tsx (sub-tab bar + section content)
@src/shared/types.ts (RestRequest, RequestBody)
@src/webview/theme.css (rm-subtabs, rm-code)
</context>

<tasks>

<task type="auto">
  <name>Implement code generators</name>
  <files>src/webview/codegen.ts (new)</files>
  <action>Export four pure functions taking a RestRequest and returning a string:
    toCurlString (reuse/adapt logic from curl.ts — import toCurl if signature fits),
    toJsFetch (fetch with method/headers/body, JSON.stringify body when body.mode
    raw json; omit body for none; include headers as object literal),
    toPythonRequests (requests.request(method, url, headers=..., json=.../data=...)),
    toGoHttp (net/http + bytes.NewReader when body present, header loop).
    Escape properly (JSON.stringify for strings). Keep bodies inline, no files.
    No comments needed.</action>
  <verify>yarn test: add unit tests in test/ (find existing test dir layout, e.g. test/webview/codegen.test.ts) asserting each generator's output for a GET with query + a JSON POST.</verify>
  <done>Four generators exist, unit-tested, deterministic output.</done>
</task>

<task type="auto">
  <name>Code sub-tab in request editor</name>
  <files>src/webview/views/RequestPanel/RequestPanel.tsx, src/webview/theme.css</files>
  <action>Extend the sub-tab array (params/headers/body/auth/tests) with
    { id: 'code', label: 'Code', icon: 'code' }. Render section: language chips
    (cURL / JS · Fetch / Python / Go) using existing .rm-chip pattern, a <pre
    class="rm-code rm-codebox-mirror">-style read-only block with the generated
    snippet for the ACTIVE request state (recompute on every render — edits
    propagate automatically), and a Copy button using navigator.clipboard like
    RequestPanel.tsx:1141 already does. Hide file-upload bodies (formdata with
    file items): render a small hint instead of a broken snippet.</action>
  <verify>yarn run typecheck; F5: open request, switch to Code, edit URL/body -> snippet updates; Copy works.</verify>
  <done>Code section present, live, copyable; formdata-files shows hint not garbage.</done>
</task>

</tasks>

<verification>
- yarn test (incl. new codegen tests)
- yarn run typecheck
- Manual F5 check described above.
</verification>

<success_criteria>
Code section with 4 languages, live-updating, copy-to-clipboard, formdata-file
corner case handled.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux/07-03-SUMMARY.md` in the phase dir.
</output>