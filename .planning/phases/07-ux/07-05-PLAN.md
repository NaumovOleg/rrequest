---
phase: 07-ux
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/extension/messaging.ts
  - src/extension/panel.ts (RouterDeps wiring)
  - src/webview/views/Sidebar/Sidebar.tsx (collection/folder settings UI)
  - src/webview/state/store.ts (tree shape already carries Collection)
autonomous: true
must_haves:
  truths:
    - User can attach pre-request and test scripts to a collection or folder
    - Scripts cascade: collection pre -> folder pre -> request pre -> send -> request test -> folder test -> collection test
    - Results of all levels show in console logs / test chips together
    - Scripts survive save/reload and Drive sync (fields ride the Collection object)
  artifacts:
    - Collection/Folder script fields in types.ts
    - Cascade execution in messaging sendRequest
    - Collection/folder settings with Scripts section
  key_links:
    - saved Collection JSON -> sync snapshot (secrets still stripped)
    - sendRequest pre-script path (line 164) extended to walk the tree
---

<objective>
Add collection-level and folder-level pre-request and test scripts, executed in
a predictable cascade around the request's own scripts.

Purpose: hoist shared auth/bootstrap logic (set token, inject headers) and
common assertions up out of every request — the main reason requests end up
duplicated. Mirrors Bruno/Postman collection scripting.
Output: schema fields + cascade execution + settings UI in the sidebar.
</objective>

<execution_context>
Types today: Collection = { id, name, workspaceId, requests, folders?,
environmentId? } and Folder = { id, name, requests } (src/shared/types.ts:111-113).
RouterDeps (src/extension/messaging.ts) has runPreScript/runTestScript and
collections.list(). The sendRequest case runs request-level scripts only.
Folder "settings" gear exists for collection rows (Sidebar.tsx gear popup);
folders have rename/delete only — the folder gear needs a settings entry added.
Sync: collections travel whole through snapshot.ts; adding optional string
fields is backward-compatible (old sync peers read missing field as '').
</execution_context>

<context>
@src/shared/types.ts (Collection, Folder at :111-113)
@src/extension/messaging.ts (sendRequest at :159; RouterDeps)
@src/extension/scripting/sandbox.ts (runPreScript/runTestScript signatures)
@src/webview/views/Sidebar/Sidebar.tsx (gear menus, settings popups)
@src/extension/sync/snapshot.ts (collection serialization)
</context>

<tasks>

<task type="auto">
  <name>Schema: script fields + cascade engine</name>
  <files>src/shared/types.ts, src/extension/messaging.ts</files>
  <action>Add optional fields preRequestScript?: string and testScript?: string
    to Collection and Folder types (src/shared/types.ts). In messaging.ts
    extract a helper resolveScripts(collections, collectionId, folderId) =>
    { pre: string[], test: string[] } that walks collection -> folder (null
    folder => just collection) and collects each level's scripts top-down.
    Then in the sendRequest case: before running the request's own pre-script,
    run collection and folder pre-scripts in order, merging logs and envSets
    (each runs through deps.runPreScript with the progressively-mutated
    request — later scripts see earlier mutations, matching Bruno sandwich
    order); on any pre-script error return the same 'Pre-request script
    failed' response as today, listing the level in the message. After the
    send, run folder -> collection test scripts (reverse depth: request test
    first, then folder, then collection — mirror Bruno's post sandwich),
    merging tests and logs into the existing payload. envSets from each level
    persist through persistEnvSets as today. Read-only (viewer) workspaces:
    scripts are EXECUTED on send regardless of role (they're not mutations of
    the tree), no change needed.</action>
  <verify>yarn test: add unit tests in test/extension/messaging (find the existing
    test dir layout; there are vitest tests for messaging per the repo). Cover:
    cascade order with spy-ish fake runPreScript/runTestScript recording call
    sequence; error in folder pre aborts send; empty scripts skip levels.</verify>
  <done>Cascade ordering verified by tests; error path aborts like today;
    envSets/logs from all levels merged into response payload.</done>
</task>

<task type="auto">
  <name>Collection & folder settings UI with Scripts</name>
  <files>src/webview/views/Sidebar/Sidebar.tsx, src/webview/theme.css</files>
  <action>In the collection gear popup add 'Scripts' entry opening a small modal
    (reuse .rm-modal pattern) with two CodeTextareas: Pre-request script and
    Test script, saving via the EXISTING saveRequest-style host mutation —
    the collection save path is saveCollection; add WebviewMessage types
    saveCollectionScript { collectionId, preRequestScript?, testScript? } and
    saveFolderScript { collectionId, folderId, preRequestScript?, testScript? }
    in src/shared/types.ts WebviewMessage union, with handlers in messaging.ts
    using withCollection-like mutation, returning the tree snapshot. Folder
    rows: extend their gear (currently rename/delete) with the same Scripts
    entry. Show a subtle indicator (script icon) on collection/folder rows that
    have any script set. Textareas: reuse CodeTextarea (elements/) with
    mono styling; scripts persist via save (draft model NOT needed here —
    modal Save button is enough, matching collection rename UX).</action>
  <verify>yarn run typecheck; F5: set a folder pre-script that sets a variable,
    a collection test-script asserting status; run a request inside -> variable
    present, PASS/FAIL chips aggregate; scripts survive reload; sync shows in
    Drive file after Sync Now (fields present, secrets unchanged behavior).</verify>
  <done>Scripts editable from sidebar for both collection and folder; run cascade
    end-to-end; persisted + synced.</done>
</task>
</tasks>

<verification>
- yarn test (new cascade tests)
- yarn run typecheck
- Manual F5 flow described in task 2 verify.
</verification>

<success_criteria>
Collection/folder scripts run in documented cascade, editable from the sidebar,
persist locally and through Drive sync; request-level behavior unchanged when
no collection/folder scripts exist.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux/07-05-SUMMARY.md` in the phase dir.
</output>