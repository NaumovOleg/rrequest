---
phase: 08-power
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/extension/messaging.ts
  - src/extension/net/ws-manager.ts
  - src/extension/net/grpc-client.ts
  - src/shared/types.ts
  - package.json
  - src/extension/extension.ts
autonomous: true
must_haves:
  truths:
    - WebSocket URL/headers and gRPC address/metadata interpolate {{vars}} at connect time like HTTP does
    - Environment secrets are stored in VS Code Secret Storage, not plaintext JSON
    - HTTP timeout is configurable (setting + per-workspace/per-request override)
  artifacts:
    - interpolate calls in wsConnect and grpcInvoke host paths
    - env secret storage (secret-toggle values kept out of env JSON)
    - rrequest.requestTimeoutMs setting plumbed through messaging -> http-client
  key_links:
    - wsConnect case -> interpolate(url, headers)
    - grpcInvoke case -> interpolate(address, metadata)
    - environment-store/panel wiring -> secrets
---

<objective>
Close the "variables only on HTTP" gap: make WebSocket and gRPC behave like
HTTP ({{var}} substitution in URL/headers/address/metadata), move secret env
values into VS Code Secret Storage so nothing sensitive sits in plaintext
JSON on disk, and make the HTTP timeout a user setting.

Purpose: consistency + security + one knob devs actually need.
</objective>

<execution_context>
- interpolate(text, vars) lives in src/extension/scripting/interpolate.ts (pure, tested).
- wsConnect handler is messaging.ts:469; grpcInvoke case is messaging.ts:615.
- activeVars() (messaging.ts:127) returns the active env's variables — reuse.
- Environment store (stores/environment-store.ts) persists envs to JSON in
  globalStorage; sync strips secrets but local disk doesn't. Add host-level
  secret flag handling: value stored via context.secrets under a key derived
  from env id + var key; JSON keeps a placeholder.
- http-client.ts DEFAULT_TIMEOUT_MS=30000; add opts.timeoutMs already exists —
  only plumbing from a setting is missing.
</execution_context>

<context>
@src/extension/messaging.ts (wsConnect ~469, grpcInvoke ~615, activeVars ~127)
@src/extension/scripting/interpolate.ts
@src/extension/stores/environment-store.ts
@src/extension/net/http-client.ts
@package.json (configuration block)
</context>

<tasks>

<task type="auto">
  <name>Interpolate for WS and gRPC</name>
  <files>src/extension/messaging.ts</files>
  <action>In the wsConnect case: const vars = await activeVars(); build url = interpolate(msg.url, vars); headers each {…key, value} via interpolate. Keep empty-vars fast-path (skip map when vars empty, mirroring http-client pattern).
  In the grpcInvoke case: interpolate msg.address, msg.proto (yes — proto can contain {{var}} for e.g. package names), msg.message? No: message body is data, not config — but Postman interpolates body too. Follow http-client: interpolate everything except raw message JSON — skip message (keep it literal; documented limitation).
  Metadata rows: interpolate key+value.
  Neither path runs scripts — structured vars only.
  </action>
  <verify>yarn test: new tests for ws/grpc message handling with a fake deps: assert interpolated url/headers reach the ws factory, and interpolated address/metadata reach grpcInvoke. Existing tests must stay green.</verify>
  <done>WS and gRPC honor {{var}} exactly like HTTP, no regressions.</done>
</task>

<task type="auto">
  <name>Secret env values -> Secret Storage</name>
  <files>src/extension/stores/environment-store.ts, src/extension/messaging.ts, src/extension/panel.ts</files>
  <action>environment-store saves Environment JSON; add a secrets dependency (SecretsLike from account-store pattern) + secretmarker:
  - When saving: for each variable with secret flag, store value under key `rrequest.env.<envId>.<i>` (i = index) and replace value with '' in the persisted JSON. For non-secret vars value stays as-is.
  - When loading: read the JSON; for each index i, if secret && missing value, hydrate from secrets.
  - Careful with mutation: keep the in-memory object values in sync so interpolation still works (messaging reads env.variables from list()).
  - Type: no new types; keying by index avoids renaming issues.
  Migration: existing envs have plaintext values in file legacy — leave them (ponytail: no migration scope creep; flag NEW secrets as such).
  </action>
  <verify>yarn test: unit: env with secret saves placeholder in file + real value in secrets store; load restores. Manual: F5 set secret var, open globalStorage file — no value; sync still strips.</verify>
  <done>Secret variables never exist in plaintext JSON after save; interpolation unchanged.</done>
</task>

<task type="auto">
  <name>Timeout setting</name>
  <files>package.json, src/extension/messaging.ts, src/extension/panel.ts</files>
  <action>package.json configuration add rrequest.requestTimeoutMs (number, default 30000, min 1000).
  panel.ts: read from workspace.getConfiguration('rrequest'); pass into deps as timeoutMs.
  messaging.ts sendRequest case: let timeoutMs = deps.timeoutMs; pass opts.timeoutMs = timeoutMs to deps.send(effective, {...}).
  http-client already honors opts.timeoutMs — just plumb it.
  Note in SUMMARY: no per-request override (YAGNI) — setting only.
  </action>
  <verify>yarn test (timeout plumbing unit: default 30000 used when unset; override used); manual: F5, set 1000ms, slow endpoint -> timeout error at ~1s.</verify>
  <done>Timeout configurable from settings; default unchanged.</done>
</task>

</tasks>

<verification>
- yarn typecheck && yarn test
- F5 manual: env with {{secret}} works in WS url; gRPC metadata with var; timeout setting on slow endpoint.
</verification>

<success_criteria>
{{var}} works in WebSocket (URL + headers) and gRPC (address + metadata);
secret environment values are invisible in the on-disk JSON after saving (stored in VS Code Secret Storage) while still resolving at send time; HTTP timeout follows the configuration setting.
</success_criteria>

<output>
After completion, create `.planning/phases/08-power/08-02-SUMMARY.md`.
</output>