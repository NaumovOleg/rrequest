---
phase: 08-power
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/extension/messaging.ts
  - src/extension/stores/collection-store.ts
  - src/webview/views/RequestPanel/RequestPanel.tsx
  - src/webview/views/Sidebar/Sidebar.tsx
autonomous: true
must_haves:
  truths:
    - User can write a description for a request and a collection (markdown)
    - Sidebar shows description presence, docs view renders it
  artifacts:
    - description?: string on CollectionItem + Collection
    - 'openRequestDocs' panel/modal rendering markdown
    - markdown renderer: simple (no new dep needed — render headings/bold/code via regex on <pre>? We have no markdown lib. Add minimal renderer or reuse existing? Check if any markdown lib is present (none in deps) -> hand-rolled minimal renderer for headings/emphasis/lists/code blocks, tested.)
  key_links:
    - saveRequest path carries description
    - Docs modal reads request/collection description
---

<objective>
Documentation inside the tool: markdown description on requests and
collections, shown in a drafts modal, with the sidebar icon/indicator when set.
This replaces the missing "request description" gap and prepares for future
docs export.

Purpose: docs where you work, not a separate site.
Output: description fields wired through save + a docs modal + minimal md renderer.
</objective>

<execution_context>
- CollectionItem = union; adding description to RestRequest|GrpcRequest|WsRequest
  means extending each member; better: add `description?: string` to each.types
  member (or a shared base — types are flat discriminated unions; keep simple:
  add to each member).
- CRUD: saveRequest + createRequest messages already carry the full object —
  host saves as-is; only type + validation touches needed.
- Docs modal: new lightweight component in RequestPanel (a side panel / modal
  displaying rendered description) invoked from toolbar icon.
</execution_context>

<context>
@src/shared/types.ts (CollectionItem members, Collection)
@src/extension/messaging.ts (saveRequest/createRequest)
@src/webview/views/RequestPanel/RequestPanel.tsx (name bar area, toolbar row)
@src/webview/views/Sidebar/Sidebar.tsx (row rendering)
</context>

<tasks>

<task type="auto">
  <name>Description plumbing (types + persistence)</name>
  <files>src/shared/types.ts, src/extension/messaging.ts, src/extension/stores/collection-store.ts</files>
  <action>types.ts: add description?: string to RestRequest, GrpcRequest, WsRequest, Collection, Folder.
  messaging.ts: saveRequest/createRequest already persist the whole object — nothing extra needed beyond type; ensure imports rebuild fine (no exhaustive switch breaks? verify).
  Also: OpenAPI export + Postman export should now (re)include description when present (simple field copy; Postman request.description, OpenAPI operation description + info.description) — touch src/extension/formats/postman.ts/openapi.ts.
  </action>
  <verify>yarn typecheck && yarn test; export round-trip tests still green; new test: description round-trip in saveRequest.</verify>
  <done>Descriptions persist and export.</done>
</task>

<task type="auto">
  <name>UI: description input + docs modal</name>
  <files>src/webview/views/RequestPanel/RequestPanel.tsx (new DescriptionDialog or side pane)</files>
  <action>Toolbar: add "📝 Docs" icon-button (visible always). Clicking opens a side pane (like AssertPanel) with:
  - textarea "Description (markdown)" bound to draft request field; Save persists via existing saveRequest flow (dirty-dot included).
  Sidebar: describe tooltip + small icon when description set on request/collection row.
  Docs modal read/edit only (viewer role: read-only per plan X).
  Minimal md renderer (no deps): headings #/##, **bold**, *italic*, `code`, ``` fenced blocks, bullet/- lists, links [t](url). Wrap in <pre> for <code> at block level; render in preview pane on the same modal (toggle Edit/Preview) — reuse existing wiring (response body preview patterns).
  </action>
  <verify>F5: set description, save, reopen => persisted; Docs pane renders md; sidebar shows icon on rows with descriptions.</verify>
</task>

</tasks>

<verification>
- yarn typecheck && yarn test
- F5 manual flows above; export with HTTP + collection desc into OpenAPI/Postman keeps field.
</verification>

<success>
Markdown descriptions on requests/collections persist, export, and render in a
writtable Docs pane.
</success>

<output>
08-06-SUMMARY.md
</output>