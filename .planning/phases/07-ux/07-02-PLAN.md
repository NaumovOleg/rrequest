---
phase: 07-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/webview/views/RequestPanel/RequestPanel.tsx
  - src/webview/state/store.ts
  - src/webview/theme.css
  - src/webview/components/History.tsx
autonomous: true
must_haves:
  truths:
    - User can re-send the last sent request with one click
    - The re-send uses the exact payload (method, url, headers, body) of what was sent
    - Re-send works even after switching tabs, without editing anything
  artifacts:
    - lastSent payload captured in store
    - "Repeat last" button in ResponsePanel header/blank state
  key_links:
    - response message arrives -> store.lastSent updated
    - button click -> postToHost sendRequest with cached request
---

<objective>
Let the user re-run the most recently executed request with a single click —
no re-editing, no tab switch.

Purpose: fastest possible iterate-send cycle when debugging backend changes.
Output: a "↻ Repeat" affordance in the response header + blank state, backed
by a store() cached copy of the last sent request.
</objective>

<execution_context>
RequestPanel currently Sends from the ACTIVE tab (RequestPanel.tsx). The store keeps
all tabs and `setResponse`. History.tsx already lists entries; reuse its
'play/re-run' affordance if one exists, otherwise the response header is the
natural home.
</execution_context>

<context>
@src/webview/state/store.ts  (Tab, active tab, send path)
@src/webview/views/RequestPanel/RequestPanel.tsx
@src/webview/views/ResponsePanel/ResponsePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Capture last sent request in store</name>
  <files>src/webview/state/store.ts, src/webview/views/RequestPanel/RequestPanel.tsx</files>
  <action>Add `lastSent: RestRequest | null` + `setLastSent` to the store. In the
    Send action handler (RequestPanel), right before postToHost({type: 'sendRequest', ...}),
    call setLastSent with a deep copy of the payload being sent. Also stash the
    response tab id. Must ignore cancelled sends: only set lastSent when send actually
    fires.</action>
  <verify>yarn test (store tests if exist) + manual: send a request, inspect store via react devtools (Zustand) that lastSent is populated.</verify>
  <done>lastSent non-null after any send; null initially; untouched by cancel.</done>
</task>

<task type="auto">
  <name>Render "Repeat" button + blank-state action</name>
  <files>src/webview/views/ResponsePanel/ResponsePanel.tsx, src/webview/theme.css</files>
  <action>In the response header (next to status pills) add a ghost icon button
    (`codicon-refresh` / "Repeat", aria-label="send last request again") enabled
    when store.lastSent != null. onClick: re-post {type:'sendRequest'} with the
    cached lastSent payload (new requestId via the same helper used by the Send
    button), and mark the in-flight state. Also add a "Repeat last request" link
    on the blank-state (.rm-blank-hint) used for empty responses. Style via
    existing .rm-btn--ghost + .rm-icon-btn patterns. ResponsePanel lives in
    src/webview/views/ResponsePanel; confirm ResponsePanel is rendered by
    RequestPanel inside rm-req-response.</action>
  <verify>Manual F5: send request, edit another tab, close it, hit Repeat -> same URL+headers+body fires; response cycles. State persists across tab switches.</verify>
  <done>Repeat button always visible in response header; blank state offers the action; in-flight shows cancel toggle (existing behavior), repeat disabled while sending.</done>
</task>

</tasks>

<verification>
- yarn test, yarn run typecheck
- Manual flow described in task 2 verify.
</verification>

<success_criteria>
Repeat last request with one click, from any tab, without editing fields.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux/07-02-SUMMARY.md` in the phase dir.
</output>