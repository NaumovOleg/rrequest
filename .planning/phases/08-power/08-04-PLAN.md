---
phase: 08-power
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/extension/messaging.ts
  - src/extension/stores/collection-store.ts
  - src/extension/stores/history-store.ts
  - src/webview/views/ResponsePanel/ResponsePanel.tsx
  - src/webview/views/RequestPanel/RequestPanel.tsx
autonomous: true
must_haves:
  truths:
    - User can save the current response as an example attached to the request
    - User can open a saved example later and diff it against a fresh response (and the reverse)
  artifacts:
    - Example type + examples[] on RestRequest
    - save/open/diff messaging cases + UI in ResponsePanel
  key_links:
    - 'saveExample(requestId, payload) -> collection-store'
    - 'openExample(requestId, idx) -> response panel rendered from example in place'
---

<objective>
Add response examples: after a send, one click saves the response shape
(status+headers+body) as an example on the request; a dropdown lets you reopen
any example and diff it against the live response. This is Postman's "save
response as example" minus the doc-marketplace part.

Purpose: regression tracking of payload shapes without a separate tool.
Output: examples on requests + diff view + tests.
</objective>

<execution_context>
- stores: collection-store saves collections on any mutation; add
  examples array to RestRequest pieces (or a separate sidecar file keyed by
  requestId? Ponytail: examples belong to the request — augment type and let
  saveRequest/cascade persist them. Full body can be large; cap example body at
  the same 64 MB full-body cache? keep it simple: store as-is up to 5MB).
- ResponsePanel renders response body; diff = render both bodies side by side
  (text diff for JSON: parse and annotate? Minimal: left/right scroll sync +
  top-level diff markers not required — show both and a toggle 'Diff' that
  produces a unified colored diff of text lines. Implement using a trivial
  LCS-free line diff? Ponytail: use simple line-by-line diff via @babel? NO —
  no new deps. Use jsdiff-free algorithm: split lines, two-pointer with
  subsequence fallback? Just render "changed lines" marks with O(n*m)? n small
  in practice. Keep to line-diff with basic longest common substring on line
  arrays; cap 20k lines.
</execution_context>

<context>
@src/shared/types.ts
@src/extension/messaging.ts (sendRequest, keepFullBody, saveBody patterns)
@src/extension/stores/collection-store.ts
@src/webview/views/ResponsePanel/ResponsePanel.tsx
@src/webview/views/RequestPanel/RequestPanel.tsx (toolbar)
</context>

<tasks>

<task type="auto">
  <name>Types + store + messages</name>
  <files>src/shared/types.ts, src/extension/messaging.ts, src/extension/stores/collection-store.ts</files>
  <action>types.ts:
    type Example = { id; at; status; statusText; headers: KeyValue[]; body: string; bodyIsBinary?; bodyBase64?; name: string }
    RestRequest gains examples?: Example[]
    WebviewMessage: {type:'saveExample', requestId, collectionId?, folderId?} | {type:'openExample', requestId, exampleId?} | {type:'deleteExample', requestId, exampleId} | {type:'diffExample', requestId, exampleId}
    HostMessage: {type:'exampleSaved', ok} | {type:'exampleOpened', exampleId?, example?: Example, diff?: boolean}
  messaging.ts:
    saveExample: find the request in collections (helper similar to trash restore search), fetch the LAST cached body from keepFullBody cache in panel scope? — body lives in the response message already being in webview; simplest: webview sends the example payload back in saveExample (no host store for bodies).
    Therefore: saveExample carries `example: Example` built in webview from the current response state.
    host: locate request by id, set examples push (cap 50), persist collection via saveCollection (existing helper).
    openExample: return the stored example to the requesting panel (sendRequest path style).
  </action>
  <verify>yarn test: save/append/load example round-trip in store; no full body crossing host (assert webview-built examples don't hit host). types compile.</verify>
  <done>Example CRUD works at the message layer.</done>
</task>

<task type="auto">
  <name>UI: save/open/diff</name>
  <files>src/webview/views/ResponsePanel/ResponsePanel.tsx</files>
  <action>In response toolbar add:
  - '💾 Save example' button (disabled when no response) -> sends saveExample with the current response payload.
  - '📋 Examples' popup listing saved examples (name = status + time); item context: Open, Diff with live response.
  - Diff view: modal/dual pane: left = example body, right = live body; simple line-diff function diffLines(a,b) colors added/removed/content-changed lines; toggle 'Pending'. Close reverts.
  - When a live response arrives and an example is open — normal response replaces pane; diff updates live.
  Keep fullBody behavior unaffected.
  </action>
  <verify>F5: send once, save example; change backend; resend; open example -> see stored; Diff -> colored differences. Reopen request later -> example list intact.</verify>
  <done>Save/open/diff works; no regressions in response toolbar; examples survive reloads.</done>
</task>

</tasks>

<verification>
- yarn typecheck && yarn test
- Manual: save → edit → diff flow, plus delete example.
</verification>

<success>
One-click example save, reopen and diff of response shapes per request,
persisted per collection. No new deps (diff handmade; capped).
</success>

<output>
08-04-SUMMARY.md
</output>